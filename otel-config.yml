receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  filter/productidonly:
    error_mode: ignore
    traces:
      span:
        - attributes["productId"] == nil
  batch:

exporters:
  logging:
    loglevel: debug
  jaeger:
    endpoint: jaeger-service:14250
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [ otlp ]
      processors: [ filter/productidonly, batch ]
      exporters: [ logging, jaeger ]
